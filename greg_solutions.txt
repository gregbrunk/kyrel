Day 1 Answers:

var initial_state = [ '.', '.', '.', '.', '.' ];

function main(n) {
  moveRight();
  moveRight();
  useBlue();
  draw();

} 

——


var initial_state = ['b', 'b', 'b', 'b', 'b'];

function main(n) {
  moveRight();
  moveRight();
  erase();

}